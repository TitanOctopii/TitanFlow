# TitanFlow Status — 2026-02-28

## Summary
- v0.3 microkernel scaffold assembled (core spine, IPC, LLM broker, gateway, telemetry).
- IPC routing includes bounded queues, TTL drop handling, and DLQ inserts.
- Watchdog + scheduler wired with readiness gating.
- Telemetry HTTP bridge and gateway service stubs added.
- Health report script added and improved with config + summary.

## Repo Setup
- Path: `/Users/kamaldatta/Projects/TitanFlow`
- Branch: `main`
- Public repo: `kamaldatta76/TitanFlow`

## Key Components
- `titanflow/v03/core.py` — v0.3 core runner
- `titanflow/v03/ipc_server.py` — bounded IPC with TTL + DLQ
- `titanflow/v03/llm_broker.py` — preemption + cache
- `titanflow/v03/watchdog.py` — sd_notify watchdog
- `titanflow/v03/gateway.py` — session + actor enforcement
- `titanflow/v03/telemetry_http.py` — `/metrics` + `/status` bridge
- `scripts/titan-health-report.sh` — ops health report

## Tests / Validation
- Acceptance test scaffolds in `tests/test_v03_*`
- `python3 -m compileall titanflow/v03` executed successfully.

## Ports / Runtime
- Telemetry HTTP: `19100` (default)
- Gateway HTTP: `18888` (default)
- DB path in service uses persistent state dir

## Privacy
- No real names or external IPs in public docs.

## Next Actions
- Add systemd unit hardening (Pass A/B).
- Extend test coverage for WAL pressure + watchdog lag.
- Wire pipeline telemetry into dashboard.

## Session Updates — 2026-02-28 15:05
- [15:05] Stabilized Ollama tool-call loop on MBA by truncating oversized tool results before reinjection.
- [15:05] Added Ollama keep-alive and optional num_ctx controls via env (TITANFLOW_OLLAMA_KEEP_ALIVE / TITANFLOW_OLLAMA_NUM_CTX).
- [15:05] Safe fallback retained for clients that don’t accept keep_alive/num_ctx.

## Commits (Session)
- fix: stabilize ollama tool calls

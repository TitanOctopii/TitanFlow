# TitanFlow Build Log — 2026-02-28

## Summary
- v0.3 kernel scaffolding consolidated with IPC, LLM broker, watchdog, and gateway wiring.
- Grounding gate + memory persistence completed and documented.
- Tool-call stability fixes added for Ollama on MBA (context-safe tool output + keep-alive).
- OpenClaw audit merged into a single roadmap and saved as ROADMAP.md.

## Timeline (local time, ET)
- 00:27 — `9ff16a3` — Implement TitanFlow v0.2 MVP IPC scaffold
- 00:31 — `f61a247` — Suppress repeated module disconnect alerts
- 00:33 — `4cefcb7` — TitanFlow v0.2.0-mvp — microkernel core + IPC + research module
- 01:44 — `1cf7f81` — docs: capture grounding gate + memory persistence
- 01:53 — `c16f677` — feat: grounding gate + chat memory
- 03:29 — `fc1affa` — bot: prompt hardening + Mamaji greeting + MBA definition + anti-hallucination + gemma3 config
- 03:29 — `15952dc` — core: telemetry HTTP bridge + dispatcher wiring
- 03:31 — `bd6084b` — core: ipc outbound loop + acceptance tests
- 03:35 — `134a212` — core: flesh out acceptance tests + ipc inbound loop
- 03:41 — `e916a4d` — core: wire inbound loop + v0.3 deploy package
- 03:48 — `f0618b7` — chore: ignore Ghost backups/themes
- 04:11 — `328673f` — core: wire sessions + llm, add gateway HTTP
- 04:24 — `372170f` — ops: fix telemetry socket + v03 service paths
- 04:25 — `08838b7` — docs: update v0.3 deployment notes
- 04:26 — `3e2216a` — ops: relax v03 runtime dir perms for telemetry
- 04:27 — `8257cfd` — docs: note telemetry socket permission fix
- 04:29 — `3b66974` — docs: record v0.3 deployment verification
- 04:30 — `7d3c32f` — docs: update v0.3 deploy steps
- 05:28 — `7b543b9` — chore: scrub public templates and docs
- 05:28 — `bf7f8d4` — chore: anonymize greeting comment
- 12:04 — `37243f7` — bot: system prompt hardening + memory guardrails
- 12:04 — `505d6d9` — ops: add titan health report script
- 12:32 — `906e5ad` — chore: ignore local mem0 client
- 12:39 — `a02dd08` — ops: add health env config + summary
- 13:01 — `38c5bd9` — docs: add status log
- 13:34 — `c2752ce` — docs: add TitanSafe roadmap
- 13:52 — `9c755b6` — docs: merge OpenClaw audits into roadmap
- 14:21 — `7187c0d` — fix: stabilize ollama tool calls
- 15:07 — `ade49b8` — docs: session status update
- 15:30 — `569e8f1` — docs: add build log 2026-02-28
- 16:00 — *(this session)* — feat: plugin system Phase 1 (SDK + loader + shell-exec + file-writer)
- 16:00 — *(this session)* — feat: CALL_TOOL format + Telegram HTML parse_mode
- 16:00 — *(this session)* — feat: defensive module init (_try_register) for mixed-version Sarge deploy

## Architectural Decisions (Today)

| # | Decision | Rationale |
|---|----------|-----------|
| 1 | **Ollama tool-call stabilization** | Truncate tool output before reinjection; add keep-alive + num_ctx env controls |
| 2 | **Keep-alive controls** | `TITANFLOW_OLLAMA_KEEP_ALIVE` (default `10m`) + `TITANFLOW_OLLAMA_NUM_CTX` reduce MBA cold-start failures |
| 3 | **Grounding gate enforcement** | External-entity queries route through DB search with refusal on empty hits |
| 4 | **Telemetry bridge** | `/metrics` + `/status` via HTTP sidecar for monitoring pipeline |
| 5 | **Plugin SDK ABCs** | Stable public `ToolPlugin`/`ModulePlugin`/`HookPlugin` + `PluginContext` — plugins never import TitanFlow internals |
| 6 | **CALL_TOOL inline format** | JSON-only constraint silences lfm2:24b; `CALL_TOOL name args` embedded directive works across all tested models |
| 7 | **Telegram HTML parse_mode** | Markdown parser breaks on unescaped `_`, `*`, backticks in LLM output; HTML mode + `html.escape()` is safe |
| 8 | **Shell-exec security modes** | `deny` / `allowlist` / `full` with permanent blocklist — MBA gets allowlist, Sarge gets extended allowlist |
| 9 | **Defensive `_try_register()`** | Sarge runs v0.2 no-arg module constructors; defensive wrapper handles both v0.1 (engine-injected) and v0.2 |

## Plugin System — Phase 1

### Files Created
- `titanflow/plugin_sdk.py` — Public SDK: `ToolPlugin`, `ModulePlugin`, `HookPlugin`, `PluginContext`
- `titanflow/plugin_manager.py` — Discovery (manifest.json), importlib loading, CALL_TOOL descriptions, `execute_tool()`
- `~/.titanflow/plugins/shell-exec/manifest.json` + `plugin.py` — Shell execution tool (3 security modes)
- `~/.titanflow/plugins/file-writer/manifest.json` + `plugin.py` — File write tool (allowlist + dotfile block)

### Files Modified
- `titanflow/config.py` — Added `PluginConfig` + `ModulesConfig.plugins`
- `titanflow/main.py` — Plugin loading at startup; defensive `_try_register()` for mixed-version modules
- `titanflow/telegram/bot.py` — HTML parse_mode + `_escape_html()`; CALL_TOOL parser; tool invocation loop (max 5 rounds); `/plugins` command
- `config/titanflow.yaml` — Added plugins section with extended Sarge allowlist
- `config/titanflow-ollie.yaml` — Added plugins section with MBA allowlist

### Bugs Fixed This Session
- `TypeError: ResearchModule.__init__() takes 1 positional argument but 2 were given` — Sarge crash on deploy; fixed with `_try_register()`
- `Path('.env').suffix == ""` dotfile extension bug — fixed to check full filename directly
- launchd `bootstrap failed: I/O error` on MBA — fixed with `launchctl unload` + `launchctl load`
- lfm2:24b empty responses on JSON-only tool call format — fixed with CALL_TOOL inline directive

## Checklist Updates
- Plugin system Phase 1: **COMPLETE** ✓ (both instances running with shell_exec + file_write tools)
- Telegram parse errors: **FIXED** ✓ (HTML mode + escape helper)
- Roadmap updated to reflect merged OpenClaw audit priorities.

## Infrastructure State (EOD)

| Component | Host | Status |
|-----------|------|--------|
| TitanFlow (Flow) | Sarge (10.0.0.33) | systemd active |
| TitanFlow (Ollie) | MBA (10.0.0.10) | launchd PID active |
| Ollama | Sarge + MBA | running |
| TitanPipeline War Room | MBA :5173 | active |
| Qdrant | TitanShadow (10.0.0.32) | running |

## Next Session Priorities
1. Ghost publish plugin for Ollie (titanflow.space)
2. War Room online indicators (heartbeat pings from TitanFlow)
3. Workflow pipeline runner Phase 1 (step runner + approval gates)
4. First pipeline chunk — assign Ollie a task to start discharge clock

